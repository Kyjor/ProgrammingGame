using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class LoadFile : MonoBehaviour {

	/*
	public Button LoadingScript;
	public string txtFile = "test";
	string txtContents;
	public InputField loadInput;
	public InputField scriptEdit;
	public Text file;

	//public void UpdateFileName(string word) {
	//GameObject.Find ("FileName").GetComponentsInChildren<Text> ().text = "Test";
	//	texts.text = word;
	//}


	// Use this for initialization
	void Start () {
		TextAsset txtAssets = (TextAsset)Resources.Load (txtFile);
		txtContents = txtAssets.text;

	}

	public void loadScript()
	{
		TextAsset txtAssets = (TextAsset)Resources.Load (txtFile);
		txtContents = txtAssets.text;

		if (txtFile == loadInput.text) {
			file.text = " " + txtContents + " ";
		}
	}
	// Update is called once per frame
	void Update () {
		
	}
	*/
}
